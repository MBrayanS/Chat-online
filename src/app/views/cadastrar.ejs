<!DOCTYPE html>
<html lang="pt-br">
    <head>
        <%- include('partials/head') %>
        <link rel="stylesheet" href="assets/styles/cadastrar.css">
        <script src="assets/scripts/cadastrar.js" defer></script>
        <title>Cadastrar</title>
    </head>

<body>
    <section class="cadastrar">

        <div class="formulario">
            <h1 class="titulo">Cadastrar</h1>

            <form>
                <label for="nome">Nome:
                    <input
                        type="text"
                        id="nome"
                        name="nomeInput"
                        required
                        minlength="3"
                        placeholder="Insira seu nome"
                        maxlength="20"
                        title="Nome de usuário deve ter entre 3 e 20 caracteres"
                    >
                </label>

                <label for="chave">Chave:
                    <input 
                        type="text" 
                        id="chave" name="chaveInput" 
                        minlength="4" 
                        maxlength="255" 
                        required 
                        placeholder="Insira sua chave"
                        pattern="^(?=.*[a-z])(?=.*[A-Z])(?=.*\W).+$"
                        title="Por favor, escolha uma chave incluindo letras maiúsculas e minúsculas, números e caracteres especiais. Tornará sua senha mais segura!"
                    >
                </label>

                <label for="senha">Senha:
                    <input
                        type="password"
                        id="senha"
                        name="senhaInput"
                        minlength="8"
                        maxlength="255"
                        required 
                        placeholder="Insira sua senha"
                        pattern="^(?=.*[a-z])(?=.*[A-Z])(?=.*\W).+$"
                        title="Por favor, escolha uma senha incluindo letras maiúsculas e minúsculas, números e caracteres especiais. Tornará sua senha mais segura!"
                    >
                </label>

                <label for="confirmarSenha">Confirmar Senha:
                    <input
                        type="password"
                        id="confirmarSenha"
                        name="confirmarSenhaInput"
                        minlength="8"
                        maxlength="255"
                        required
                        placeholder="Insira sua senha"
                        pattern="^(?=.*[a-z])(?=.*[A-Z])(?=.*\W).+$"
                        title="Por favor, escolha uma senha incluindo letras maiúsculas e minúsculas, números e caracteres especiais. Tornará sua senha mais segura!"
                    >
                </label>

                <button type="submit" class="enviar">Cadastrar</button>
            </form>

            <p>Já possui uma conta?</p>
            <a href="logar">Faça login</a>
        </div>

    </section>
</body>
</html>